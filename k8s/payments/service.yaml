apiVersion: v1
kind: Service
metadata:
  name: payments-service
  namespace: payments
  labels:
    app: payments
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: payments

